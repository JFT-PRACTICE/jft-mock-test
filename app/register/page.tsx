"use client"; import { useState } from "react"; export default function RegisterPage() { const [name, setName] = useState(""); const [studentId, setStudentId] = useState(""); const [password, setPassword] = useState(""); function register(e: React.FormEvent) { e.preventDefault(); if (!name.trim() || !studentId.trim() || !password.trim()) { alert("Please fill all fields"); return; } const students = JSON.parse( localStorage.getItem("students") || "[]" ); const existingStudent = students.find( (student: any) => student.studentId.toLowerCase() === studentId.trim().toLowerCase() ); if (existingStudent) { alert("This Student ID is already registered."); return; } const newStudent = { name: name.trim(), studentId: studentId.trim(), password, blocked: false, }; students.push(newStudent); localStorage.setItem( "students", JSON.stringify(students) ); alert("Account created successfully!"); window.location.href = "/login"; } return ( <main className="min-h-screen flex items-center justify-center bg-gray-100 p-5"> <div className="w-full max-w-md bg-white p-8 rounded-2xl shadow"> <h1 className="text-3xl font-bold text-center"> Create Student Account </h1> <form onSubmit={register} className="mt-8 space-y-4" > <input type="text" placeholder="Full Name" value={name} onChange={(e) => setName(e.target.value)} className="w-full border p-3 rounded-lg" /> <input type="text" placeholder="Student ID" value={studentId} onChange={(e) => setStudentId(e.target.value)} className="w-full border p-3 rounded-lg" /> <input type="password" placeholder="Password" value={password} onChange={(e) => setPassword(e.target.value)} className="w-full border p-3 rounded-lg" /> <button type="submit" className="w-full bg-black text-white p-3 rounded-lg" > Create Account </button> </form> <a href="/login" className="block text-center text-blue-600 mt-5" > Login </a> </div> </main> ); }