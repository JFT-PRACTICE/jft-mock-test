"use client"; import { useEffect, useState } from "react"; const defaultQuestions = [ { id: "default-1", section: "Vocabulary", question: "「みず」को अर्थ के हो?", options: ["Water", "Food", "Rice", "Tea"], answer: 0, audio: "", }, { id: "default-2", section: "Vocabulary", question: "「せんせい」को अर्थ के हो?", options: ["Student", "Teacher", "Friend", "Doctor"], answer: 1, audio: "", }, { id: "default-3", section: "Grammar", question: "わたし ___ ネパールじんです。", options: ["は", "を", "に", "で"], answer: 0, audio: "", }, { id: "default-4", section: "Grammar", question: "ごはんを ___。", options: ["のみます", "たべます", "いきます", "みます"], answer: 1, audio: "", }, { id: "default-5", section: "Reading", question: "「おはよう」は कहिले प्रयोग गरिन्छ?", options: ["Morning", "Afternoon", "Night", "Before sleeping"], answer: 0, audio: "", }, { id: "default-6", section: "Reading", question: "「さようなら」को अर्थ के हो?", options: ["Welcome", "Goodbye", "Thank you", "Sorry"], answer: 1, audio: "", }, { id: "default-7", section: "Listening", question: "「こんにちは」को अर्थ के हो?", options: ["Good morning", "Hello", "Good night", "Thank you"], answer: 1, audio: "", }, { id: "default-8", section: "Listening", question: "「ありがとう」को अर्थ के हो?", options: ["Sorry", "Hello", "Thank you", "Goodbye"], answer: 2, audio: "", }, ]; export default function AdminPage() { const [students, setStudents] = useState<any[]>([]); const [results, setResults] = useState<any[]>([]); const [questions, setQuestions] = useState<any[]>([]); const [search, setSearch] = useState(""); const [selectedStudent, setSelectedStudent] = useState<string | null>(null); const [section, setSection] = useState("Vocabulary"); const [question, setQuestion] = useState(""); const [option1, setOption1] = useState(""); const [option2, setOption2] = useState(""); const [option3, setOption3] = useState(""); const [option4, setOption4] = useState(""); const [answer, setAnswer] = useState("0"); const [audio, setAudio] = useState(""); const [editingId, setEditingId] = useState<string | null>(null); useEffect(() => { const admin = localStorage.getItem("adminLoggedIn"); if (admin !== "true") { window.location.href = "/admin/login"; return; } loadData(); }, []); function loadData() { const savedStudents = JSON.parse( localStorage.getItem("students") || "[]" ); const savedResults = JSON.parse( localStorage.getItem("studentResults") || "[]" ); const savedQuestions = localStorage.getItem("mockQuestions"); setStudents(savedStudents); setResults(savedResults); if (savedQuestions) { setQuestions(JSON.parse(savedQuestions)); } else { setQuestions(defaultQuestions); localStorage.setItem( "mockQuestions", JSON.stringify(defaultQuestions) ); } } function clearQuestionForm() { setSection("Vocabulary"); setQuestion(""); setOption1(""); setOption2(""); setOption3(""); setOption4(""); setAnswer("0"); setAudio(""); setEditingId(null); } function saveQuestion() { if ( !question.trim() || !option1.trim() || !option2.trim() || !option3.trim() || !option4.trim() ) { alert("Please fill all question fields."); return; } if (editingId) { const updatedQuestions = questions.map((item) => { if (item.id === editingId) { return { ...item, section, question: question.trim(), options: [ option1.trim(), option2.trim(), option3.trim(), option4.trim(), ], answer: Number(answer), audio: audio.trim(), }; } return item; }); localStorage.setItem( "mockQuestions", JSON.stringify(updatedQuestions) ); setQuestions(updatedQuestions); clearQuestionForm(); alert("Question updated successfully!"); return; } const newQuestion = { id: Date.now().toString(), section, question: question.trim(), options: [ option1.trim(), option2.trim(), option3.trim(), option4.trim(), ], answer: Number(answer), audio: audio.trim(), }; const updatedQuestions = [ ...questions, newQuestion, ]; localStorage.setItem( "mockQuestions", JSON.stringify(updatedQuestions) ); setQuestions(updatedQuestions); clearQuestionForm(); alert("Question added successfully!"); } function editQuestion(item: any) { setEditingId(item.id); setSection(item.section); setQuestion(item.question); setOption1(item.options[0]); setOption2(item.options[1]); setOption3(item.options[2]); setOption4(item.options[3]); setAnswer(String(item.answer)); setAudio(item.audio || ""); window.scrollTo({ top: 0, behavior: "smooth", }); } function deleteQuestion(id: string) { const confirmDelete = confirm( "Are you sure you want to delete this question?" ); if (!confirmDelete) return; const updatedQuestions = questions.filter( (item) => item.id !== id ); localStorage.setItem( "mockQuestions", JSON.stringify(updatedQuestions) ); setQuestions(updatedQuestions); if (editingId === id) { clearQuestionForm(); } } function getStudentResults(studentId: string) { return results.filter( (result) => result.studentId === studentId ); } function getLatestResult(studentId: string) { const studentResults = getStudentResults(studentId); if (studentResults.length === 0) { return null; } return studentResults[studentResults.length - 1]; } function toggleBlock(studentId: string) { const updatedStudents = students.map((student) => { if (student.studentId === studentId) { return { ...student, blocked: !student.blocked, }; } return student; }); localStorage.setItem( "students", JSON.stringify(updatedStudents) ); setStudents(updatedStudents); } function deleteStudent(studentId: string) { const confirmDelete = confirm( "Are you sure you want to delete this student?" ); if (!confirmDelete) return; const updatedStudents = students.filter( (student) => student.studentId !== studentId ); const updatedResults = results.filter( (result) => result.studentId !== studentId ); localStorage.setItem( "students", JSON.stringify(updatedStudents) ); localStorage.setItem( "studentResults", JSON.stringify(updatedResults) ); setStudents(updatedStudents); setResults(updatedResults); setSelectedStudent(null); } function logout() { localStorage.removeItem("adminLoggedIn"); window.location.href = "/admin/login"; } const filteredStudents = students.filter((student) => { const text = search.toLowerCase(); return ( student.name.toLowerCase().includes(text) || student.studentId.toLowerCase().includes(text) ); }); return ( <main className="min-h-screen bg-gray-100 p-5"> <div className="max-w-7xl mx-auto space-y-6"> <div className="bg-white rounded-2xl shadow p-6"> <div className="flex flex-col md:flex-row md:justify-between md:items-center gap-4"> <div> <h1 className="text-3xl font-bold"> Admin Dashboard </h1> <p className="text-gray-500 mt-1"> JFT Mock Test Management </p> </div> <button onClick={logout} className="bg-red-600 text-white px-4 py-2 rounded-lg" > Logout </button> </div> </div> <div className="bg-white rounded-2xl shadow p-6"> <h2 className="text-2xl font-bold"> Question Management </h2> <p className="text-gray-500 mt-1"> Add, edit and delete mock test questions </p> <div className="mt-6 grid gap-4"> <select value={section} onChange={(e) => setSection(e.target.value)} className="w-full border p-3 rounded-lg" > <option>Vocabulary</option> <option>Grammar</option> <option>Reading</option> <option>Listening</option> </select> <textarea placeholder="Write question here..." value={question} onChange={(e) => setQuestion(e.target.value)} className="w-full border p-3 rounded-lg min-h-24" /> <input type="text" placeholder="Option 1" value={option1} onChange={(e) => setOption1(e.target.value)} className="w-full border p-3 rounded-lg" /> <input type="text" placeholder="Option 2" value={option2} onChange={(e) => setOption2(e.target.value)} className="w-full border p-3 rounded-lg" /> <input type="text" placeholder="Option 3" value={option3} onChange={(e) => setOption3(e.target.value)} className="w-full border p-3 rounded-lg" /> <input type="text" placeholder="Option 4" value={option4} onChange={(e) => setOption4(e.target.value)} className="w-full border p-3 rounded-lg" /> <select value={answer} onChange={(e) => setAnswer(e.target.value)} className="w-full border p-3 rounded-lg" > <option value="0"> Correct Answer: Option 1 </option> <option value="1"> Correct Answer: Option 2 </option> <option value="2"> Correct Answer: Option 3 </option> <option value="3"> Correct Answer: Option 4 </option> </select> <input type="url" placeholder="Listening Audio URL (optional)" value={audio} onChange={(e) => setAudio(e.target.value)} className="w-full border p-3 rounded-lg" /> <div className="flex flex-col md:flex-row gap-3"> <button onClick={saveQuestion} className="flex-1 bg-green-600 text-white p-3 rounded-lg font-bold" > {editingId ? "Update Question" : "Add Question"} </button> {editingId && ( <button onClick={clearQuestionForm} className="bg-gray-200 px-6 py-3 rounded-lg font-bold" > Cancel Edit </button> )} </div> </div> <div className="mt-8"> <h3 className="text-xl font-bold"> Current Questions ({questions.length}) </h3> <div className="mt-4 space-y-3"> {questions.map((item, index) => ( <div key={item.id} className="border rounded-xl p-4" > <div className="flex flex-col lg:flex-row lg:justify-between gap-4"> <div> <p className="text-sm text-blue-600 font-bold"> {item.section} </p> <p className="font-bold mt-1"> {index + 1}. {item.question} </p> <p className="text-gray-500 mt-2"> 1. {item.options[0]} </p> <p className="text-gray-500"> 2. {item.options[1]} </p> <p className="text-gray-500"> 3. {item.options[2]} </p> <p className="text-gray-500"> 4. {item.options[3]} </p> <p className="text-green-600 font-bold mt-2"> Correct: Option {item.answer + 1} </p> {item.audio && ( <audio controls src={item.audio} className="mt-3 max-w-full" /> )} </div> <div className="flex gap-2 h-fit"> <button onClick={() => editQuestion(item)} className="bg-blue-600 text-white px-4 py-2 rounded-lg" > Edit </button> <button onClick={() => deleteQuestion(item.id)} className="bg-red-600 text-white px-4 py-2 rounded-lg" > Delete </button> </div> </div> </div> ))} </div> </div> </div> <div className="bg-white rounded-2xl shadow p-6"> <div className="flex flex-col md:flex-row md:justify-between md:items-center gap-4"> <h2 className="text-2xl font-bold"> Registered Students ({students.length}) </h2> <input type="text" placeholder="Search name or Student ID..." value={search} onChange={(e) => setSearch(e.target.value)} className="border p-3 rounded-lg w-full md:w-80" /> </div> {filteredStudents.length === 0 ? ( <p className="text-gray-500 mt-5"> No student found. </p> ) : ( <div className="mt-5 space-y-5"> {filteredStudents.map((student) => { const latestResult = getLatestResult(student.studentId); const studentResults = getStudentResults(student.studentId); const isOpen = selectedStudent === student.studentId; return ( <div key={student.studentId} className="border rounded-xl p-5" > <div className="flex flex-col lg:flex-row lg:justify-between gap-5"> <div> <p className="text-lg font-bold"> {student.name} </p> <p className="text-gray-500"> Student ID: {student.studentId} </p> <p className="mt-2"> Status:{" "} <span className={ student.blocked ? "text-red-600 font-bold" : "text-green-600 font-bold" } > {student.blocked ? "Blocked" : "Active"} </span> </p> </div> <div className="bg-gray-50 rounded-xl p-4"> <p className="font-bold"> Latest Test Result </p> {latestResult ? ( <> <p className="mt-2"> Score:{" "} <span className="font-bold"> {latestResult.score} /{" "} {latestResult.total} </span> </p> <p> Percentage:{" "} <span className="font-bold"> {latestResult.percentage}% </span> </p> <p className="text-sm text-gray-500"> {latestResult.date} </p> </> ) : ( <p className="text-gray-500 mt-2"> No test taken yet </p> )} </div> <div className="flex flex-wrap gap-2 items-center"> <button onClick={() => setSelectedStudent( isOpen ? null : student.studentId ) } className="bg-blue-600 text-white px-4 py-2 rounded-lg" > {isOpen ? "Hide History" : "View History"} </button> <button onClick={() => toggleBlock(student.studentId) } className="bg-yellow-500 text-white px-4 py-2 rounded-lg" > {student.blocked ? "Unblock" : "Block"} </button> <button onClick={() => deleteStudent(student.studentId) } className="bg-red-600 text-white px-4 py-2 rounded-lg" > Delete </button> </div> </div> {isOpen && ( <div className="mt-5 border-t pt-5"> <h3 className="font-bold text-lg"> Test History </h3> {studentResults.length === 0 ? ( <p className="text-gray-500 mt-3"> No test attempts yet. </p> ) : ( <div className="mt-3 space-y-3"> {studentResults.map( (result, index) => ( <div key={index} className="bg-gray-50 rounded-lg p-4" > <div className="flex flex-col md:flex-row md:justify-between gap-2"> <p className="font-bold"> Attempt {index + 1} </p> <p> Score:{" "} <span className="font-bold"> {result.score} /{" "} {result.total} </span> </p> <p> Percentage:{" "} <span className="font-bold"> {result.percentage}% </span> </p> <p className="text-gray-500"> {result.date} </p> </div> </div> ) )} </div> )} </div> )} </div> ); })} </div> )} </div> </div> </main> ); }