"use client"; import { useEffect, useState } from "react"; export default function DashboardPage() { const [student, setStudent] = useState<any>(null); const [results, setResults] = useState<any[]>([]); useEffect(() => { const savedStudent = localStorage.getItem("loggedInStudent"); if (!savedStudent) { window.location.href = "/login"; return; } const currentStudent = JSON.parse(savedStudent); setStudent(currentStudent); const allResults = JSON.parse( localStorage.getItem("studentResults") || "[]" ); const myResults = allResults.filter( (result: any) => result.studentId === currentStudent.studentId ); setResults(myResults); }, []); if (!student) { return null; } return ( <main className="min-h-screen bg-gray-100 p-5"> <div className="max-w-5xl mx-auto"> <div className="bg-white rounded-2xl shadow p-8"> <h1 className="text-3xl font-bold"> Student Dashboard </h1> <p className="mt-3 text-gray-600"> Welcome, {student.name} </p> <p className="mt-1 text-gray-500"> Student ID: {student.studentId} </p> <a href="/mock-test" className="inline-block bg-black text-white px-6 py-3 rounded-lg mt-8" > Start Mock Test </a> <div className="mt-10"> <h2 className="text-2xl font-bold"> My Result History </h2> {results.length === 0 ? ( <p className="text-gray-500 mt-4"> You have not taken any test yet. </p> ) : ( <div className="mt-5 space-y-3"> {results.map((result, index) => ( <div key={index} className="border rounded-xl p-5 bg-gray-50" > <div className="flex flex-col md:flex-row md:justify-between gap-3"> <div> <p className="font-bold"> Attempt {index + 1} </p> <p className="text-gray-500 mt-1"> {result.date} </p> </div> <p> Score:{" "} <span className="font-bold"> {result.score} / {result.total} </span> </p> <p> Percentage:{" "} <span className="font-bold"> {result.percentage}% </span> </p> </div> </div> ))} </div> )} </div> <button onClick={() => { localStorage.removeItem("loggedInStudent"); window.location.href = "/login"; }} className="block mt-8 text-red-600" > Logout </button> </div> </div> </main> ); }